#!/bin/bash

set -o errexit
set -o pipefail
set -o nounset

# Skip collectstatic for dev - handle static files locally
echo "Skipping collectstatic for development..."

# Run migrations
python /app/manage.py migrate

# Start development server
exec python /app/manage.py runserver 0.0.0.0:8000